<%= form_for(@alarm_task) do |f| %>
  <% if @alarm_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alarm_task.errors.count, "error") %> prohibited this alarm_task from being saved:</h2>

      <ul>
      <% @alarm_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :finished %><br />
    <%= f.check_box :finished %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
